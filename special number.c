#include<stdio.h>
main()
{
	int i,rem,s=0;
	printf("entre any number");
	scanf("%d",&n);
	{
		while(i<=n)
		if(n%i==0);
		s=s+i;
	}
	if(s==n)
	printf("given number is special number");
	else
	printf("not a special number");
	
}
